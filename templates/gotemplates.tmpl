---
{
    "streamId": "pages-locations", 
    "urlFormat": "{{ .id }}"
}
---
{{define "gotemplates.tmpl" -}}
<html>
    <body>
        <h1> Welcome to {{.name}}</h1>
        <p>Description: 
           {{ .description }}         
        </p>
    </body> 
</html>
{{end}}
